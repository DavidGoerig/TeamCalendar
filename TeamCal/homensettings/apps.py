from django.apps import AppConfig

"""
    Link for the Home and settings part.
    It basically contain the home and the users
"""

class HomensettingsConfig(AppConfig):
    name = 'homensettings'
